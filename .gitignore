# .gitignore for Docker Monitoring Stack

# --- Docker specific ignores ---
# Ignore Docker Compose build cache
.dockerignore
.env.build

# Ignore Docker Compose generated files that are not part of the source
docker-compose.override.yml
docker-compose.local.yml

# Ignore Docker volumes data
# These are named volumes, so they typically live outside the project directory,
# but it's good practice to list them if they ever get created locally.
grafana-data/
prometheus-data/
loki-data/

# Ignore any .env files that might contain sensitive credentials
# We specifically created 'monitoring.env' for this stack, so ignore it.
.env
*.env
monitoring.env

# Ignore logs generated by containers if they persist locally
# (Promtail typically forwards them, but local files might appear)
logs/
*.log

# Ignore temporary files
*.tmp
*.temp

# Ignore editor/IDE specific files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo

# Ignore compiled Python files (if any Python scripts are added directly)
*.pyc
__pycache__/

# Ignore OS generated files
.DS_Store
Thumbs.db